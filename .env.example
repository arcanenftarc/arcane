# Public site URL used for canonical / Open Graph metadata.
# Never put secrets or API keys in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_SITE_URL=https://arcanenft.xyz

# Stage 1 does not use a backend. These are placeholders for later stages.
# Keep real secrets in server-only env vars (no NEXT_PUBLIC_ prefix).
# X_CLIENT_ID=
# X_CLIENT_SECRET=
# DATABASE_URL=
# MINT_CONTRACT_ADDRESS=
